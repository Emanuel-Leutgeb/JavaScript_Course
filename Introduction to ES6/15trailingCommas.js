function add(param1, ){  //<== , a trailing comma (not good for code paradigma) but it works in for now
    const example = {
        name: "Emanuel"
    };

    console.log(example);
}

add("something");